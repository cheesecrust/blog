
## komi-2539 mcp 아이템 접근성

- mcp card 가 tab 이 가능하도록 하고, 포커싱 시 무엇을 포커싱했는지 알도록

`<button>` 의 경우에 기본적으로 tab 으로 focus 가 가능하기 때문에 이를 제외하기 위해서 `tabIndex={-1}` 을 추가하였습니다. 

`<div>` 의 경우에는 이 기능이 안 켜져 있어서 `tabIndex={0}` 으로 순서 안에 넣어 주었습니다.

> tab 의 포커스 순서는 dom 트리 기준으로 위에서 부터 아래로 차례 차례 갑니다.

## komi-2540 페이지 네이션 번호에 접근성 추가

- `span` 은 키보드 포커스가 불가능해서 `button` 으로 변경
>  이때 tabIndex 를 추가하지 않고 button 으로 바꾼 이유는 다른 부수적인, onKeyDown, 스크린 리더에게 전달해 주기 위한 속성들을 추가해야 하기 때문에 간단한 button 으로 변경했습니다.

- 페이지가 넘어가는 경우 focus 가 해당 페이지의 첫 mcp item 으로 가는 것이 자연스럽기 때문에 이렇게 구현

> **`isLoading`만 있으면:**
>- 현재는 아니지만 추후 react quey 에서 캐싱을 한다면 `isLoading`이 `false → false`로 변하지 않아 effect가 실행되지 않음
> **`list`만 있으면:**
>- 로딩 시작 시 `list`가 빈 배열로 바뀌면서 effect가 실행됨
>- 이때 카드가 아직 없어서 포커스 실패 + `isPageChanged`가 `false`로 리셋
>- 실제 데이터 로드 후에는 플래그가 이미 꺼져서 포커스가 안 잡힘
>aria-disabled 는 비활성 상태라고 알려주는...

##  komi-2541 정렬 드롭다운 접근성

- 드롭 다운 메뉴를 키보드로 다룰때에 있어서 방향키등으로 컨트롤 가능하도록 변경
listbox 패턴을 준수

- 추후에 다른 dropdown 박스를 위해서 hook 으로 분리해서 진행

hook 으로 분리하고 props 들도 hook 에서 반환하는 형태로 만들었습니다.

sort_option 을 밖으로 뺄때의 이점 : 매 랜더 마다 생성하는 것을 막는다.

이때 props getter 패턴을 사용하였습니다.

focus 를 하면 해당 focus 의 순서 정보는 브라우저에서 저장합니다.

> e.preventDefault(): qm


## komi-2560 포커스 복원 기능

뒤로 가기시에 전 화면에 있던 곳으로 focus 를 옮겨주기 위함
이때 필요한 것은 

1. 저장 할 곳
	1. sessionStorage 를 선택

2. 무엇을 저장할지
	1. map 의 형태로 저장 각각의 locatinoKey 를 키
	2. value 는 고유한 값을 부여 or 추출 

3. 저장 하는 시점
	1. 저장하는 시점은 포커스가 이동할 때마다 저장하도록 하였습니다.
	2. 이때 네비바나 푸터의 경우는 랜더가 되지 않고 계속 남아있는 형태여서 비우도록 하였습니다.

그리고 계속 focus-id 를 부여하면 귀찮을거 같아서 했는데, 만약의 고유값으로 간다면 필요가 없을지도?

## komi-2608 

dialog 를 오픈하였을때 뒤에 focus 가 남게되는 문제가 있어서 시작

우선 dialog 열기 전에 어디에 focus 였는지를 저장

안에 다른 enter 말고 다른 동작이 없는데 되는 이유는 링크는 a 태그로 브라우저의 기본 동작 arrow up down 도 브라우저의 기그러면 이때 


